<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/corPrimary</item>
        <item name="colorPrimaryDark">@color/corPrimaryDark</item>
        <!--item name="navigationBarColor">#0288D1</item-->
        <item name="colorAccent">@color/corAccent</item>
    </style>

    <style name="MyThemaAppBar" parent="ThemeOverlay.AppCompat.Dark">
        <item name="android:textColorPrimary">#FFFFFF</item>
        <item name="android:textColorSecondary">#FFFFFF</item>
    </style>
</resources>
